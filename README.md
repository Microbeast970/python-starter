# python-starter

A small, ready-to-run Python starter using `uv`, `pytest`, and `ruff`.

## Quick start

```powershell
cd C:\Users\mattd\Documents\code\python-starter
uv venv
uv pip install -e ".[dev]"
.venv\Scripts\activate
python -m python_starter
pytest
ruff check .
```
